import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  /// Confirmed from Figma UI page (`29:430`) Splash / Onboarding / Auth frames.
  static const Color background = Color(0xFF121312);
  static const Color primary = Color(0xFFF6BD00);
  static const Color onPrimary = Color(0xFF121312);
  static const Color onBackground = Color(0xFFFFFFFF);
  static const Color onBackgroundSecondary = Color(0x99FFFFFF);
  static const Color surface = Color(0xFF121312);
  static const Color onSurface = Color(0xFFFFFFFF);
  static const Color border = Color(0xFFF6BD00);
  static const Color error = Color(0xFFCF6679);

  /// Confirmed auth input fill from Login/Register/Forget Password frames.
  static const Color inputFill = Color(0xFF282A28);

  /// Confirmed overlay endpoint on OnBoarding `38:172`.
  static const Color onboardingOverlayRed = Color(0xFF85210E);
}
